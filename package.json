{
  "name": "arc-suite",
  "private": true,
  "workspaces": [
    "landing",
    "treasury",
    "reputation",
    "marketplace"
  ],
  "scripts": {
    "landing": "npm run dev --workspace=landing",
    "treasury": "npm run dev --workspace=treasury",
    "reputation": "npm run dev --workspace=reputation",
    "marketplace": "npm run dev --workspace=marketplace",
    "dev:all": "concurrently \"npm run treasury\" \"npm run reputation\" \"npm run marketplace\"",
    "build:all": "npm run build --workspace=landing && npm run build --workspace=treasury && npm run build --workspace=reputation && npm run build --workspace=marketplace",
    "lint:all": "npm run lint --workspace=landing && npm run lint --workspace=treasury && npm run lint --workspace=reputation && npm run lint --workspace=marketplace",
    "monitor:prod": "node scripts/monitor.mjs",
    "test:smoke": "node scripts/smoke.mjs"
  },
  "overrides": {
    "postcss": "8.5.10"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
